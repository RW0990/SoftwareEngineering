<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Dashboard</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <link rel="stylesheet" href="/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Michroma&display=swap"
      rel="stylesheet" />
  </head>

  <body class="dashboard">

    <%- include('partials/nav') %>


    <% if (message) { %>
  <p class="success-message"><%= message %></p>
<% } %>
    <h1 id="dashboardtitle">Upcoming Shows</h1>

    <% bandSite.forEach(show => { %>

    <div class="details">

      <h2>
        <%= show.showTitle %> - <%= show.location %>
      </h2>

      <p>
        <%= show.showDate.toDateString() %>
      </p>

      <p>
        <%= show.venue %> - <%= show.location %>
      </p>

      <p>
        <%= show.description %>
      </p>

      <p>
        Ticket price: <span class="euro">&euro;</span><%= show.price %>
      </p>

      <a href="/events">
        <button type="button">
          Buy Tickets
        </button>
      </a>

    </div>

    <% }) %>

  </body>
</html>